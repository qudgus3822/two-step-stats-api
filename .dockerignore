# [변경: 2026-08-24 11:55, 김병현 수정] 도커 빌드 컨텍스트 축소 + 비밀정보 유출 방지용 신규 작성.

# 깃 / 에이전트 설정
.git
.gitignore
.claude
CLAUDE.md

# 의존성 (이미지 안에서 npm ci 로 새로 깔기 때문에 호스트 것은 절대 안 넣는다)
node_modules

# 로컬 빌드 산출물 (이미지 안에서 다시 빌드)
dist

# 환경변수 — 이미지에 절대 굽지 않는다. Cloud Run 환경변수/Secret Manager 로 주입할 것
.env
.env.*
!.env.example

# 런타임 업로드 데이터
data

# 도커 자체
Dockerfile
.dockerignore
docker-compose*.yml

# 로그 / 캐시 / 에디터 / OS
*.log
npm-debug.log*
coverage
.eslintcache
.vscode
.idea
.DS_Store
*.tsbuildinfo
